# Planejamento local do projeto (não vai para o repositório)
docs/

# Texto bruto extraído dos PDFs, regenerável a qualquer momento
scripts/extract/raw/

# Dependências e build (Node.js / Next.js)
node_modules/
.next/
out/
build/
/coverage
*.tsbuildinfo
next-env.d.ts

# Variáveis de ambiente
.env
.env.local
.env.*.local

# Sistema operacional / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.pem

# Imagens enviadas pelo painel (ficam no disco do servidor e entram no backup)
storage/

# Pacotes gerados para enviar ao servidor
/release/
